# Install on top of requirements.txt for local development / CI:
#   pip install -r requirements.txt -r requirements-dev.txt

pytest==8.3.4                 # pytest-asyncio 0.24.x requires pytest<9 — see note below
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx==0.28.1                 # FastAPI TestClient transport
faker==33.1.0                 # synthetic evaluation/test data
